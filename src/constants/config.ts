// holds constant variables that are not part of the .env.local file

// api provider key, matches provider name to id in the database api_provider table
export const apiProviderKey = {
    'quickbooks': 1,
    'plaid': 2,
};